pre-commit:
  parallel: true
  jobs:
    - name: format
      grob: "*.{css,json,md}"
      run: npx prettier --write --ignore-unknown {staged_files}
      stage_fixed: true
      skip:
        - merge
        - rebase

pre-push:
  parallel: true
  jobs:
    - name: format
      grob: "*.{css,json,md}"
      run: npx prettier --write --ignore-unknown {staged_files}
      stage_fixed: true
      skip:
        - merge
        - rebase
